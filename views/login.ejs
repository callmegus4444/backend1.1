<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=\, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10">
      <h3 class="text-3xl text-white mb-5"> login your account </h3>  
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-500 text-white p-2 mb-4" id="error-message">
            <%= error %>
        </div>
        <script>
            // Redirect to the login page after 3 seconds
            setTimeout(() => {
                window.location.href = '/login';
            }, 3000);
        </script>
    <% } %>   
      <form  autocomplete='off' action="/login" method= "post">
<input class="px-2 py-2 rounded-md outline-none bg-transperent  border-2  border-zinc-800" type ="email" placeholder="email" name="email">
<input class="px-2 py-2 rounded-md outline-none bg-transperent  border-2  border-zinc-800"  type ="password" placeholder="password" name="password">
<input class="px-5 rounded-md py-2 bg-green-500" type ="submit" value=" login account" >
        </form>
    </div>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            document.getElementById("login-form").reset();
        });
    </script>
</body>
</html>