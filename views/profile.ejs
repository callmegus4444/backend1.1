<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=\, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10">
        <div class="w-full flex item-end justify-end">
            <a class= "bg-red-500 rounded-md px-5 py-2 text-sm mb-5 inline-block"href="/logout"> 
                Logout</a>
        </div>
        <div class="flex items-start gap-3">
        <div class="w-20 h-20  rounded-md overflow-hidden">
            <img class="w-full h-full object-cover" src="images/uploads/<%=user.profilepic %>">
        </div>
      <h3 class="text-3xl text-white mb-5"><span class="font-light">Hello</span> ,<%=user.name %> </h3> 
    </div> 
       <h5 class="text-white mt-2 mb-5">you can create a new post</h5> 
       <form method = "post" action="/post">
        <textarea  placeholder="Whats in your mind ?" name ="content" class=" w-1/3 resize-none bg-transparent border-2 border-zinc-600 rounded-md  text-white">
        </textarea>
        <input class="px-5  w-45 rounded-md py-2 bg-blue-500  text-sm block mt-2" type ="submit" value="Create post" >
       </form>
       <div class="posts mt-20">
        <h3 class="text-zinc-400 text-2xl ">Your post</h3>
        <div class="postcontainer mt-5">
            <% user.posts.reverse().forEach(function(post){%> 
                <div class="post w-1/3 p-4 mb-5 bg-zinc-700 rounded-md border-2 border-zinc-600 text-white">
                <h4 class="text-blue-500 mb-2">@<%=user.username%></h4>
                <p class="text-sm text-white tracking-tight"><%= post.content %></p>
           <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                <div class="btns flex gap-5 mt-5">
            <a class="text-blue-500" href="/like/<%= post._id %>">
            <%= post.likes.indexOf(user._id)=== -1 ?"Like":"Unlike" %>
            </a>
            <a class="text-zinc-500" href="/edit/<%= post._id %>">edit</a>
            </div>
        </div>
                <%
         })%>
        </div>
       </div>
    </div>
</body>
</html>